import React, { useEffect, useState } from 'react';
import ReactMarkdown from 'react-markdown';

export default function RemoteReadme() {
    const [content, setContent] = useState('');

    useEffect(() => {
        fetch('https://raw.githubusercontent.com/zkmopro/mopro/refs/heads/main/circom-prover/README.md')
            .then(res => res.text())
            .then(text => setContent(text));
    }, []);

    return <div>
        <div class="theme-admonition theme-admonition-info admonition_o5H7 alert alert--info">
            <p>The file is auto generated from loading main branch of <a href="https://github.com/zkmopro/mopro/blob/main/circom-prover/README.md">circom-prover</a></p>
        </div>
        <ReactMarkdown>{content}</ReactMarkdown>
    </div>;
}
