import React, { useEffect, useState } from 'react';
import ReactMarkdown from 'react-markdown';

export default function RemoteReadme() {
    const [content, setContent] = useState('');

    useEffect(() => {
        fetch('https://raw.githubusercontent.com/chancehudson/rust-witness/refs/heads/main/README.md')
            .then(res => res.text())
            .then(text => setContent(text));
    }, []);

    return <div>
        <div class="theme-admonition theme-admonition-info admonition_o5H7 alert alert--info">
            <p>The file is auto generated from loading main branch of <a href="https://github.com/chancehudson/rust-witness/blob/main/README.md">rust-witness</a></p>
        </div>
        <ReactMarkdown>{content}</ReactMarkdown>
    </div>;
}